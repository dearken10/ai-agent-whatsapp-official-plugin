.env
.env.dev
.env.prod
node_modules/
tmp/
.claude/
# but ship the template's .claude (skills get seeded into each workspace)
!claude-plugin/workspace-template/.claude/
!claude-plugin/workspace-template/.claude/**

# backend runtime state (sqlite files)
backend/data/

# claude-plugin per-user runtime state
claude-plugin/data/
claude-plugin/workspaces/
claude-plugin/logs/
claude-plugin/*.log
